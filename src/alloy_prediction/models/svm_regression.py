"""
TODO:
- Implement an SVRModel wrapper derived from BaseRegressor class.
- For loading data use Hea_data_loader module for now or if anyone wanna improve/change it can do so
- Hyperparameters:
    - kernel
    - C
    - epsilon
    - gamma
    - degree
- Implement:
    - __init__()
    - fit(X_train, y_train)
    - predict(X)
    - score(X, y)
- Support multiple kernels.
- Add save() and load().
- Expose hyperparameter dictionary for optimization algorithms.
"""