"""
TODO:
- Implement a LinearRegressionModel class derived from BaseRegressor class.
- For loading data use Hea_data_loader module for now or if anyone wanna improve/change it can do so
- Accept hyperparameters (fit_intercept, regularization if needed).
- Implement:
    - __init__()
    - fit(X_train, y_train)
    - predict(X)
    - score(X, y)
- Return predictions as NumPy arrays.
- Store learned coefficients and intercept.
- Add save() and load() methods.
- Add feature scaling compatibility.
"""

